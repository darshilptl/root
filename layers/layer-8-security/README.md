# Layer 8 — Security & Privacy

## Responsibility
Every action Root can take is bounded by what you have permitted. Enforced at the architectural layer — not a runtime check. Cannot be overridden by any layer above it.

## What this layer owns
Permission enforcement, privacy boundary, audit trail.

## Scope
Cross-cutting. Every execution call passes through here unconditionally.

## Status
Specification complete. Implementation private.
Build starts after Interwave ships its core.

---
*Part of the Root Layer 0–9 architecture.*
*Full specification — root/spec/architecture.md*